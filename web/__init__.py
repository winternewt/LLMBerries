"""The researcher's window onto the ring.

Everything in here is on the puppeteer side: it reads the artifacts and the live
engine with full knowledge — true body states, unheard words, misreadings — and it
never writes a string a player could read. The launch schema is closed vocabulary
(enums, ints, bools) so that stays structural rather than a review rule.
"""
